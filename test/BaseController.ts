// import * as assert from 'assert'

// import { BaseController } from '..'

/*describe('BaseController', () => {
  it('SHOULD NOT be a constructor', () => {
    let hasError = false

    try { new BaseController() }
    catch(err) { hasError = true }

    assert.equal(hasError, true)
  })
})*/
